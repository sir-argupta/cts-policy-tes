You are a senior speech AI systems engineer.

My current system:
- Produces stable translated videos
- Uses OpenAI TTS (tts-1-hd)
- Has fixed drift and alignment
- Outputs synchronized dubbed video

Now extend it with:

1) Multi-speaker voice mapping (P0)
2) Per-language voice presets (P0)
3) Emotional tone control (P1)

Implement these professionally and cleanly.

--------------------------------------------------
### OBJECTIVES

1) Preserve speaker identity across languages
2) Make voices sound native per language
3) Improve expressiveness
4) Maintain sync stability
5) Keep backward compatibility

--------------------------------------------------
### PART 1: MULTI-SPEAKER DETECTION

Implement speaker diarization.

Preferred approach:

1) If ASR provides speaker labels → use them
2) Else integrate pyannote.audio (CPU-compatible model)
3) Else fallback to heuristic turn detection

Create:

pipeline/speakers.py

class SpeakerSegment:
    speaker_id: str
    start: float
    end: float
    text: str

Output:

List[SpeakerSegment]

Ensure segments align with transcript.

--------------------------------------------------
### PART 2: VOICE REGISTRY

Create centralized voice registry.

File:

config/voices.yaml

Example:

languages:
  en:
    male: alloy
    female: aria
    neutral: verse
  es:
    male: luna
    female: sol
  hi:
    male: indra
    female: kavya

defaults:
  fallback: alloy

--------------------------------------------------
### PART 3: SPEAKER → VOICE ASSIGNMENT

Implement:

pipeline/voice_map.py

Logic:

1) Detect speaker gender (if possible)
2) Map:
   (speaker_id, language, gender) → voice
3) Persist mapping per job
4) Keep mapping stable across segments

Store in:

output/{job_id}/speaker_map.json

Example:

{
  "SPEAKER_0": {"lang": "en", "voice": "alloy"},
  "SPEAKER_1": {"lang": "en", "voice": "aria"}
}

--------------------------------------------------
### PART 4: EMOTIONAL TONE CLASSIFICATION

Implement tone detection.

Create:

pipeline/emotion.py

For each segment:

Use Claude or OpenAI LLM to classify:

neutral
happy
excited
sad
angry
serious
empathetic

Use minimal prompt.

Cache results.

--------------------------------------------------
### PART 5: EMOTION → TTS CONTROL

Implement emotion control via:

- Prompting
- SSML-like tags
- Prosody parameters
- OpenAI voice instructions

Example mapping:

happy → faster + higher pitch
sad → slower + lower pitch
excited → emphasis

Implement:

pipeline/prosody.py

Function:

build_tts_prompt(text, emotion) -> str

--------------------------------------------------
### PART 6: PIPELINE INTEGRATION

Update pipeline:

Transcript
 ↓
Speaker detection
 ↓
Translation
 ↓
Emotion detection
 ↓
Voice mapping
 ↓
TTS with emotion + voice
 ↓
Alignment

Ensure ordering is correct.

--------------------------------------------------
### PART 7: CONFIGURATION

Add ENV:

ENABLE_SPEAKER_MAP=true
ENABLE_EMOTION=true
VOICE_CONFIG_PATH=config/voices.yaml

All features must be toggleable.

--------------------------------------------------
### PART 8: FAILSAFE LOGIC

If any step fails:

- Fallback to neutral emotion
- Fallback to default voice
- Log warning
- Continue job

Never block pipeline.

--------------------------------------------------
### PART 9: METADATA OUTPUT

Extend outputs:

output/{job_id}/

Add:

speaker_segments.json
speaker_map.json
emotion_map.json
voice_usage.json

For debugging and audits.

--------------------------------------------------
### PART 10: PERFORMANCE

- Batch emotion classification
- Cache per segment
- Parallelize where safe
- Avoid N^2 lookups

--------------------------------------------------
### PART 11: CLI + API EXTENSIONS

Add CLI flags:

--enable-speakers
--enable-emotion
--voice-preset custom.yaml

API:

POST /jobs
{
  "enable_speakers": true,
  "enable_emotion": true
}

--------------------------------------------------
### PART 12: TESTING

Create tests:

- Multi-speaker podcast
- Interview
- Classroom lecture

Verify:

- Voice consistency
- Emotion stability
- No drift regression

--------------------------------------------------
### PART 13: README UPDATE

Document:

- Speaker mapping
- Voice presets
- Emotion tuning
- Debugging

--------------------------------------------------
### QUALITY TARGET

After implementation:

✓ Same speaker = same voice  
✓ No language mismatch  
✓ Emotions audible  
✓ No sync loss  
✓ No voice switching bugs  

--------------------------------------------------
### IMPORTANT

- Do not over-engineer
- Avoid unnecessary ML dependencies
- Use existing pipeline style
- Write readable code
- Comment heuristics

--------------------------------------------------
### FIRST OUTPUT

Start by:

1) Showing architecture changes
2) New files
3) Updated pipeline flow
4) Example outputs

Then output full code.
