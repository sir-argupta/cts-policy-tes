You are a senior AI systems engineer.

My current local project implements:
- ASR
- Translation
- Subtitle generation
- Open-source TTS
- Audio alignment
- Muxing

Now I have access to OpenAI’s `tts-1-hd` model.

Your task: COMPLETELY migrate the system to use OpenAI TTS only.

Remove all Coqui / pyttsx3 / Piper code.

Refactor cleanly.

--------------------------------------------------
### GOALS

1) Replace all TTS logic with OpenAI `tts-1-hd`
2) Simplify audio generation
3) Improve voice quality
4) Standardize configuration
5) Keep local pipeline working

--------------------------------------------------
### TTS ARCHITECTURE

Create:

pipeline/tts.py

with:

class OpenAITTSProvider:
    def synthesize(
        self,
        text: str,
        voice: str,
        out_path: str,
        speed: float | None = None
    ) -> None

Use OpenAI Audio Speech API.

Support voices:
- alloy (default)
- verse
- aria
- luna (if available)

Allow voice selection via ENV.

--------------------------------------------------
### API USAGE

Use official OpenAI Python SDK.

Use model:

tts-1-hd

Format: wav

Sample rate: 24000 Hz

Streaming: OFF (file-based generation)

Retry on failures.

Timeout: 60s.

--------------------------------------------------
### PIPELINE UPDATE

Update pipeline to:

For each segment:

1) Send text → OpenAI TTS
2) Receive WAV
3) Save seg_X_raw.wav
4) Measure duration
5) Fit to target duration (existing align logic)
6) Save seg_X.wav

No more local models.

--------------------------------------------------
### CONFIGURATION

Add ENV:

OPENAI_API_KEY (required)
OPENAI_TTS_VOICE=alloy
OPENAI_TTS_MODEL=tts-1-hd

Remove all TTS_PROVIDER logic.

--------------------------------------------------
### DEPENDENCIES

Update requirements.txt:

REMOVE:
- TTS
- pyttsx3
- piper
- soundfile (if unused)
- torch (if present)

ADD / ENSURE:

openai>=1.12.0
httpx
tenacity
pydub
numpy

Keep ffmpeg external.

--------------------------------------------------
### ERROR HANDLING

Implement:

- Retry 3x (tenacity)
- Handle:
  - 429
  - 5xx
  - timeout
- Automatic backoff

If TTS fails:
- Insert silence
- Log error
- Continue job

--------------------------------------------------
### COST CONTROL

Add basic logging:

- characters per segment
- estimated cost
- total per job

Write to:

cost.json

--------------------------------------------------
### PERFORMANCE

- Parallelize TTS calls (ThreadPoolExecutor)
- Max 4 concurrent OpenAI calls
- Respect rate limits

--------------------------------------------------
### CODE CLEANUP

Remove:

- Any open-source TTS classes
- Auto-detection logic
- Fallback logic
- Old dependencies
- Dead code

Ensure:

- One TTS provider
- One interface
- One config path

--------------------------------------------------
### CLI UPDATE

Add flags:

--voice alloy
--tts-model tts-1-hd

Override ENV.

--------------------------------------------------
### README UPDATE

Document:

- OpenAI TTS setup
- API key
- Voice options
- Pricing
- Rate limits

--------------------------------------------------
### QUALITY TARGET

Result should:

- Sound natural
- No robotic artifacts
- Stable pacing
- No clipping
- No pops

--------------------------------------------------
### IMPORTANT

- Use real OpenAI SDK code
- No mockups
- No pseudo-code
- Minimal abstractions
- Production-ready

--------------------------------------------------
### FIRST OUTPUT

Start by:

1) Showing removed files
2) Showing new/modified files
3) Explaining migration
4) Showing updated requirements.txt

Then output full code.
